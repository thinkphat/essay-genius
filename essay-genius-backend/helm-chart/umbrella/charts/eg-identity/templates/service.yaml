apiVersion: v1
kind: Service
metadata:
  name: {{ include "eg-identity.fullname" . }}
  namespace: {{ .Values.namespace}}
  labels:
    app: {{ include "eg-identity.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ .Values.service.mainPort.name }}
      port: {{ .Values.service.mainPort.port }}
      targetPort: {{ .Values.service.mainPort.targetPort }}
      protocol: {{ .Values.service.mainPort.protocol }}

  {{- range .Values.service.extraPorts }}
    - name: {{ .name }}
      protocol: {{ .protocol }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
  {{- end }}
  selector:
    app: {{ include "eg-identity.name" . }}
