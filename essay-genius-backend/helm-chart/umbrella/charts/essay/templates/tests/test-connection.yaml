apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "essay.fullname" . }}-test-connection"
  labels:
    {{- include "essay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "essay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
