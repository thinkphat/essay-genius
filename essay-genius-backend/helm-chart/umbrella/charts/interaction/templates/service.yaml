apiVersion: v1
kind: Service
metadata:
  name: {{ include "interaction.fullname" . }}
  labels:
    {{- include "interaction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "interaction.selectorLabels" . | nindent 4 }}
